using Godot;
using System;

public partial class DragDropTarget : Control
{
    /*public override bool _CanSystemDropData(Vector2 position, Array types)
    {
        label.AddThemeColorOverride("bg_color", Colors.Yellow);
        label.Text = "File Drop Area! \n(DATATYPE::FILES) \n" + position + "\n" + (types.Contains(1) ? "Can drop!" : "Can't drop...");
        return types.Contains(1);
    }*/
}
